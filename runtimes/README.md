# Runtimes

Put selected Reticulum/LXMF runtime folders here.

Example layout:

```text
runtimes/rns-1.2.5/
  RNS/
  LXMF/
  venv/
```

The skeleton falls back to local stubs when real modules are missing.
